// Basic smoke test. Real E2E tests come later when auth + data fixtures are set up.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('sanity', () {
    expect(1 + 1, 2);
  });
}
